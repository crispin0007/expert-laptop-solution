"""
AI Assistant — EventBus listeners (Phase 3 stubs).

These will handle events from other modules once Phase 3 is active.
"""
from __future__ import annotations
import logging

logger = logging.getLogger(__name__)


# @listens_to('ai.command.executed', module_id='ai_assistant')
# def on_command_executed(payload: dict, tenant) -> None:
#     """Log or audit an executed AI command (Phase 3)."""
#     pass


# @listens_to('ai.command.failed', module_id='ai_assistant')
# def on_command_failed(payload: dict, tenant) -> None:
#     """Handle a failed AI command — notify user or fallback (Phase 3)."""
#     pass
